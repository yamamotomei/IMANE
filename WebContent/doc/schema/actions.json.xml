<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="doc.xslt" ?>
<schema name="actions.json">
	<schemadef>http://json-schema.org/draft-06/schema#</schemadef>
	<title>actions.json:アクションカードのセット</title>
	<description><![CDATA[アクションカードのセットを定義します。
		アクションカードは、参加者または自動応答ユーザが自発的に開始することのできる行動を表現します。
		アクションにはtypeプロパティで指定される次の種類があります。
		- share: インシデント情報(ステートカード)を他のユーザに伝達することを目的としたアクション。すべてのシナリオに共通。
		- notification: システムからの参加者へのメッセージ通知のために使用されるアクション。すべてのシナリオに共通。
		- talk: ユーザ間の任意の会話に使用されるアクション。すべてのシナリオに共通。
		- action: 特定のロールを持つユーザが実行できるアクション。シナリオごとに定義する。
		- auto: 自動応答ユーザまたはシステムが演習の進行に応じて実行するアクション。シナリオ毎に定義する。]]>
	</description>
	<type>object</type>
	<id>#fricore-schema-actions</id>
	<properties>
		<version>
			<title>バージョン情報</title>
			<description>このデータのバージョン情報を記述します。</description>
			<required>string</required>
		</version>
		<actions>
			<type>array</type>
			<uniqueItems>true</uniqueItems>
			<minItems>1</minItems>
			<description>アクションカードを定義します。</description>
			<title>アクションカード</title>
			<items>
				<required>name,type,id</required>
				<properties>
					<name>
						<type>string</type>
						<minLength>1</minLength>
						<title>アクションの表示名</title>
						<description>アクションカードの表示名を設定します。この名前はイベント一覧に表示されます。</description>
					</name>
					<type>
						<type>string</type>
						<enum>action,share,notification,talk,auto</enum>
						<default>action</default>
						<title>アクションの種類</title>
						<description>アクションの種類を指定します。action,share,notification,talk,autoが使用可能です。
						</description>
						<minLength>1</minLength>
					</type>
					<id>
						<type>string</type>
						<description>アクションを識別する一意な文字列を設定します。(必須)</description>
						<title>アクションID</title>
						<uniqueItems>true</uniqueItems>
						<required>string</required>
						<minLength>1</minLength>
					</id>
					<attach>
						<title>添付</title>
						<description>アクションにステートカードを添付することを許可する場合はtrueを指定します。省略値:false
						</description>
						<type>boolean</type>
					</attach>
					<roles>
						<title>所有ロール</title>
						<description>このアクションを使用可能なロールのIDの配列を設定します。allを設定するとすべてのユーザが使用可能となります。(必須)
						</description>
						<optional>false</optional>
						<type>array</type>
						<default>all</default>
						<items>
							<type>string</type>
						</items>
					</roles>
					<description>
						<title>説明</title>
						<description>アクションの説明を記述します。</description>
						<optional>true</optional>
						<type>string</type>
					</description>
					<attachments>
						<title>ステートカード添付</title>
						<description>アクションに添付するステートカードのIDを指定します。省略値:なし</description>
						<type>array</type>
						<optional>true</optional>
						<items>
							<type>number</type>
						</items>
					</attachments>
					<phase>
						<title>フェーズ</title>
						<description>このアクションが利用可能なフェーズを設定します。フェーズ番号の配列を指定します。省略値：すべてのフェーズで使用可能
						</description>
						<type>array</type>
						<items>
							<type>number</type>
						</items>
					</phase>
					<to>
						<type>string</type>
						<title>宛先</title>
						<description>自動アクションの宛先ロールを指定します。type:autoの場合は必須です。</description>
					</to>
					<from>
						<type>string</type>
						<title>送信元</title>
						<description>自動アクションの送信元ロールを指定します。type:autoの場合は必須です。</description>
					</from>
					<addstate>
						<title>追加システムステート</title>
						<description>アクションの受付時に追加するシステムステートを設定します。</description>
						<type>array</type>
						<items>
							<type>string</type>
						</items>
					</addstate>
					<removestate>
						<title>削除システムステート</title>
						<description>アクションの受付時に削除するシステムステートを設定します。</description>
						<type>array</type>
						<items>
							<type>string</type>
						</items>
					</removestate>
					<comment>
						<title>コメント</title>
						<description>アクションに添付するメッセージのデフォルト値を設定します。省略値:なし</description>
						<type>string</type>
					</comment>
					<statecondition>
						<title>ステート条件</title>
						<description>アクションを使用するために必要なステートカードのIDのセットを指定します。ユーザは、このプロパティに指定されたすべてのステートカードを入手しないと、「新規アクション」画面からこのアクションを選択することができません。省略値:なし(常に使用可能)
						</description>
						<type>array</type>
						<items>
							<type>string</type>
						</items>
					</statecondition>
					<timecondition>
						<title>経過時間条件</title>
						<description>前提条件となるアクションを実行後に一定時間経過後にアクションの実行を許可するために使用します。省略値:なし(常に使用可能)
						</description>
						<type>array</type>
						<items>
							<prerequisite>
								<title>前提アクション</title>
								<description>アクションを実行するための前提条件となるアクションのIDを指定します。</description>
								<type>string</type>
							</prerequisite>
							<elapsed>
								<title>経過時間</title>
								<description>条件となるアクション実行後の経過時間(秒)を指定します。</description>
								<type>number</type>
							</elapsed>
						</items>
					</timecondition>
					<delay>
						<title>自動アクション遅延時間</title>
						<description>自動アクションが実行可能になってから実行するまでの遅延時間(秒)を指定します。</description>
						<type>number</type>
					</delay>
					<systemstatecondition>
						<title>システムステート条件</title>
						<description>自動アクション(type:auto)の実行の契機となるシステムステートのIDと論理演算子を指定します。省略値:なし(無条件に実行する)
						</description>
						<type>array</type>
						<items>
							<type>string</type>
						</items>
					</systemstatecondition>
				</properties>
			</items>
		</actions>
	</properties>
	<required>actions</required>
</schema>
