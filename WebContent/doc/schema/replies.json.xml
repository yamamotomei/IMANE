<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="doc.xslt" ?>
<schema name="replies.json">
	<schemadef>http://json-schema.org/draft-06/schema#</schemadef>
	<title>reply.json:リプライカードのセット</title>
	<description>
		アクションまたはトリガーに対する応答を定義します。
		リプライはアクションに対する応答として発生するイベントの雛型を定義します。リプライはまた、アクションに対して条件によって異なる応答を行うための条件を含みます。
		アクションに対するリプライは、次のように決定されます。
		-
		自動応答ユーザに対してアクションが要求されると、アクションは一旦キューに保存され、応答待ち状態となります。アクションはすべてのリプライの処理が完了するとキューから削除されます。
		キューに保存されたアクションは一定時間ごとにリプライ処理の評価を受けます。まず、アクションIDと宛先ロールIDの組み合わせに対して、一致するリプライが候補として選択されます。
		-
		候補となったリプライは、さらにorderプロパティによってグループ化されます。orderプロパティの小さなリプライがまず候補グループとして選択されます。
		-
		システムは、候補グループ中で、statecondition、timeconditionで指定された条件を満たすリプライを一定時間ごとに探索します。条件にマッチするものがあればリプライを送信し、orderプロパティが次に大きな値を持つリプライ候補グループを以降の評価対象とします。1つ以上の候補が存在し、条件にマッチするリプライがなければ、応答は保留され、一定時間経過後に再度評価されるのを待ちます。候補がひとつもなければ、リプライ処理は終了し、アクションはキューから削除されます。
		アクションに対して候補となるリプライが一つもない場合は、デフォルトのリプライが発行され、アクションはキューから削除されます。

		同時に条件を満たすリプライが複数ある場合(たとえば、orderプロパティの値が同じでstateconditionが異なるリプライがあり、指定されたstateconditionが同時に満足する場合)は、先に評価されたリプライが適用され、残りのリプライは破棄されます。
	</description>
	<type>object</type>
	<id>#fricore-schema-replies</id>
	<properties>
		<version>
			<title>バージョン情報</title>
			<description>バージョン情報を記述します。</description>
			<required>string</required>
		</version>
		<states>
			<type>array</type>
			<uniqueItems>true</uniqueItems>
			<minItems>1</minItems>
			<title>リプライリスト</title>
			<description>フェーズとリプライのセットを定義します。</description>
			<items>
				<required>phase</required>
				<properties>
					<phase>
						<title>フェーズ</title>
						<description>フェーズ番号を定義します。</description>
						<type>string</type>
						<minLength>1</minLength>
					</phase>
					<reply>
						<title>リプライのセット</title>
						<description>phaseで指定したフェーズで使用するリプライの配列を設定します。</description>
						<type>array</type>
						<uniqueItems>true</uniqueItems>
						<minItems>1</minItems>
						<items>
							<required>actionid,to</required>
							<properties>
								<actionid>
									<title>アクションID</title>
									<description>このリプライの応答先となるアクションのIDを指定します。トリガーイベントを定義する場合は's0000'を指定します。必須
									</description>
									<type>string</type>
									<minLength>1</minLength>
								</actionid>
								<to>
									<title>宛先ロールID</title>
									<description>このリプライの応答先となるユーザのロールIDを指定します。すべてのユーザを指定する場合はallを指定します。必須
									</description>
									<type>string</type>
									<minLength>1</minLength>
								</to>
								<from>
									<title>送信元ロール</title>
									<description>トリガイベントを発行するときに送信元となるロールIDを指定します。定義済みロールのほか、systemを使用します。
									</description>
									<type>string</type>
								</from>
								<hidden>
									<title>非表示</title>
									<description>trueを指定すると、演習中にこのリプライをイベント一覧に表示しません。シナリオ制御のためのダミーのリプライを定義するときに使用します。省略値:false(表示する)
									</description>
									<type>boolean</type>
								</hidden>
								<name>
									<title>名前</title>
									<description>短い表示名を指定します。</description>
									<type>string</type>
								</name>
								<state>
									<title>添付ステートカード</title>
									<description>リプライに添付するステートカードのIDを指定します。トリガーアクションに対しては必須です。省略値:なし
									</description>
									<type>string</type>
									<minLength>1</minLength>
								</state>
								<statecondition>
									<title>ステート条件</title>
									<description>リプライが有効となるシステムステートの条件を指定します。システムステートのIDと論理演算子(AND/OR/NAND/NOR/NOT)を指定します。省略値:なし(無条件にヒット)
									</description>
									<type>array</type>
									<items>
										<type>string</type>
									</items>
								</statecondition>
								<addstate>
									<title>追加システムステート</title>
									<description>リプライを発行するときに、ここで指定したシステムステートを追加します。省略値:なし
									</description>
									<type>array</type>
									<items>
										<type>string</type>
									</items>
								</addstate>
								<removestate>
									<title>削除システムステート</title>
									<description>リプライを発行するときに、ここで指定したシステムステートを削除します。省略値:なし
									</description>
									<type>array</type>
									<items>
										<type>string</type>
									</items>
								</removestate>
								<message>
									<title>メッセージ</title>
									<description>ユーザに通知する表示用の応答メッセージを記述します。省略値:なし</description>
									<type>string</type>
									<minLength>0</minLength>
								</message>
								<constraints>
									<title>非推奨</title>
								</constraints>
								<timecondition>
									<title>非推奨</title>
								</timecondition>
								<order>
									<title>評価順</title>
									<description>n-way型シーケンスで使用する、リプライの評価順を指定します。同じorderを持つリプライをグループ化し、orderの順に評価し、ヒットしたらリプライを返して次のorderをもつ評価対象のグループを評価します。省略値:1
									</description>
									<type>number</type>
								</order>
								<delay>
									<title>応答遅延</title>
									<description>アクションを受け付けてからリプライを返すまでの秒数を指定します。省略値は0(アクションに対して即座に応答する)です。
									</description>
									<default>10</default>
									<type>number</type>
								</delay>
								<elapsed>
									<title>経過時間</title>
									<description>トリガーイベントに使用します。フェーズ開始からの経過時間(秒数)を指定します。省略値:0
									</description>
									<type>number</type>
								</elapsed>
								<type>
									<title>種類</title>
									<description>リプライの種類を指定します。'hidden'を指定すると、イベント一覧への表示を抑制します。'null'をは予約語です。省略値:なし
									</description>
									<type>string</type>
									<enum>,hidden,null</enum>
								</type>
							</properties>
						</items>
					</reply>
				</properties>
			</items>
		</states>
		<message>
			<type>array</type>
			<uniqueItems>true</uniqueItems>
			<minItems>1</minItems>
			<items>
				<required>id,text</required>
				<properties>
					<id>
						<type>string</type>
						<minLength>1</minLength>
					</id>
					<text>
						<type>string</type>
						<minLength>1</minLength>
					</text>
				</properties>
			</items>
		</message>
	</properties>
	<required>states,message</required>
</schema>
